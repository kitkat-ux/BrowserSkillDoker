{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScreenshotParams",
  "description": "Parameters for `tool.screenshot`. Omit `ref` to capture the visible tab; pass an `@e<N>` ref from the last `tool.snapshot` to crop to that element.",
  "type": "object",
  "required": [
    "session_id"
  ],
  "properties": {
    "ref": {
      "description": "Optional `@e<N>` ref allocated by the last `tool.snapshot`. Accepts both `\"e3\"` and `\"@e3\"`.",
      "type": [
        "string",
        "null"
      ]
    },
    "session_id": {
      "type": "string"
    },
    "tab_id": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int64"
    }
  }
}
